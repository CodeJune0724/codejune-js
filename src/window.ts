export default {

    /**
     * 获取屏幕高度
     *
     * @return 屏幕高度
     * */
    getHeight(): number {
        return window.innerHeight;
    },

    /**
     * 获取屏幕宽度
     *
     * @return 屏幕宽度
     * */
    getWidth(): number {
        return window.innerWidth;
    }

};